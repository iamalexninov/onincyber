import styles from "./Testimonials.module.scss";

const Testimonials = () => {
  return <section className={`section ${styles.testimonials}`}>
    <span className="subtitle">testimonials</span>
    <h2>Clients Feedback</h2>
  </section>;
};

export default Testimonials;
